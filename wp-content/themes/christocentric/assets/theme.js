/** Theme helpers for WooCommerce product grid */
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.ccr-rental-fields').forEach(function (wrap) {
        wrap.classList.add('mt-4', 'space-y-3');
    });
});
